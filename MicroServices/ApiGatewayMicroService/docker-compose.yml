# MicroServices/ApiGatewayMicroService/docker-compose.yml
networks:
  eshop-on-web-net:
    external: true

services:
  gateway:
    build: .
    labels:
      co.elastic.logs/enabled: "true"
      co.elastic.logs/service: "api-gateway"
      co.elastic.logs/env: "dev"
    ports:
      - "8090:8080"            
    environment:
      ORDERS_BACKENDS:  http://order:8001
      CATALOG_BACKENDS: http://catalog-api:8000
    networks:
      eshop-on-web-net:
        aliases:
          - gateway           
    restart: unless-stopped
